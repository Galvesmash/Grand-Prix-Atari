function movePlayer() {
  /*if (moveLeft && moveRight){
    moveLeft = false;
    moveRight = false;
    return;
  }

  if (moveLeft) { //<-
    posX-=moveSpeed;
    if (posX < 1) {
      posX = 1;
    }
    player.style.left = posX + "px";
  }
  else if (moveRight) { //->
    posX+=moveSpeed;
    if (posX > widthLimit) {
      posX = widthLimit;
    }
    player.style.left = posX + "px";
  }*/
}